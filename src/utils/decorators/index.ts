import Controller from "./controller.decorator";
import { Get, Post, Patch, Put, Delete } from "./handler.decorator";
import { ValidateBody, ValidateQuery } from './http.decorator';

export {
  Controller,
  Get,
  Post,
  Patch,
  Put,
  Delete,
  ValidateBody,
  ValidateQuery,
};